// Compose Stability Configuration
// core:healthcare 모듈의 클래스들은 Compose 모듈이 아니라 컴파일러가 안정성을 추론할 수 없음
// 여기에 등록하면 Compose 컴파일러가 안정(Stable)으로 취급함
// 조건: 모든 필드가 val이고 원시타입/String만 포함 (List 등 불안정 타입 없음)
com.example.healthcare.domain.model.RunHistory
com.example.healthcare.domain.model.RoutePoint
